// Definição e manipulação de vetores
valores = [1, 6, 5, 9, 2];
// valores = new Array(1, 6, 5, 9, 2);
valores.sort();
console.log(valores);
console.log(`Valor na posição 3: ${valores[3]}`);

// Baseados na classe Array
// Método de ordenação sort
// Iteração através de forEach
// Métodos map e reduce
