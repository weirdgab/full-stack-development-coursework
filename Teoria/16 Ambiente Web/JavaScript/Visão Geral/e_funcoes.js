// Formato tradicional (antigo)
function exibirSoma(a, b) {
	let c = a + b;
	alert(`A soma será ${c}`);
}

// Arrow function (novo)
const exibirSoma = (a, b) => {
	let c = a + b;
	alert(`A soma será ${c}`);
};
